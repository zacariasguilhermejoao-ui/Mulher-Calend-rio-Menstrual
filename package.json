{
  "name": "ciclo-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo Android project prepared; npm run sync",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
